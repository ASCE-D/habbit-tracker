/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://insightaction.pureapps.tech/',
  generateRobotsTxt: true,
  // optional configurations
  changefreq: 'daily',
  priority: 0.7,
  // exclude paths if needed
  // exclude: ['/admin/*', '/private/*'],
}